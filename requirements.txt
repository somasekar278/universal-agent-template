# Core dependencies for SOTA Agent - Realtime Insights & Alerts

# Data validation and schemas
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0
packaging>=23.0  # For semantic version parsing

# Agent frameworks
langgraph>=0.2.0
langchain>=0.2.0
langchain-core>=0.2.0

# Databricks integrations
databricks-sdk>=0.20.0
mlflow>=2.10.0

# LLM serving
openai>=1.0.0  # Compatible with Databricks Model Serving
anthropic>=0.20.0

# MCP (Model Context Protocol)
mcp>=0.1.0

# Vector store / Memory
# lakebase - add version when available

# Telemetry and observability
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-instrumentation>=0.41b0
prometheus-client>=0.19.0

# Message bus options
nats-py>=2.6.0  # NATS JetStream
redis>=5.0.0

# Web framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
websockets>=12.0
starlette>=0.27.0

# DSPy for prompt optimization
dspy-ai>=2.4.0

# TextGrad for system prompt optimization
textgrad>=0.1.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=12.0.0  # For Delta Lake / Unity Catalog

# Utilities
python-dotenv>=1.0.0
tenacity>=8.2.0  # Retry logic
httpx>=0.25.0  # Async HTTP client
pyyaml>=6.0  # For adapter config files

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
faker>=20.0.0  # For synthetic data generation

# Development tools
black>=23.0.0
ruff>=0.1.0
mypy>=1.5.0
ipython>=8.12.0
jupyter>=1.0.0

# Type stubs
types-PyYAML>=6.0.0
types-redis>=4.6.0

